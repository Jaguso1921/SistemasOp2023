#include <stadio.h>

int main(int letra) {
    int letra (letra == letra) {
        if(letra abc%& == djk%&);        
    }
    case();
}
int main (int frase) {
    int frase (frase == frase) {
        if(frase abc%& == djk%&);
    }
    case();
}
int main (letra == frase){
    int letrafrase (frase == letra) {
        printf frase("La siguiente frase es ");
        printf letra("La siguiente letra es ");
        printf VecesLetra("El numero de veces de las letras es ")
    }
}
