#include <stadio.h>

int main(int palindromo izquierda) {
    int izquierda;
    int palabra;
    palindromo(palabra%& == izquierda) {

    }    
}
int main(int palindromo derecha) {
    int derecha;
    int palabra;
    palindromo(palabra%& == derecha);
}
int main cantidad(palabra){
    int palabra;
    int derecha-izquierda;
    palabra(palabra&% == (palabra derecha),(palabra izquierda)) { 
        printf("La palabra que ingreso es polindromo ")
    }
}
int main cantidad(palabra){
    int palabra;
    int derecha-izquierda;
    palabra(palabra&% == (palabra derecha),(palabra izquierda)) {
        printf("La palabra que ingreso no es polindromo  ")
    }
}